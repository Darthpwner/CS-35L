#!/import/bin/python

def myFunction(number):
    #Integers and Floats
    tempInt = 5
    temFloat = 4.5

    #Strings
    sampleSTR = "My first python script"
    print sampleSTR
    sampleSTR = sampleSTR + 'A' + sampleSTR * 2

class Engine:
    def _init_(self, ID):
        self.modelID = ID
